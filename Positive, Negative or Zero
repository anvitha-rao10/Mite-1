#include <stdio.h>
int main()
{
    int n;
    printf("Enter the number: ");
    scanf("%d", &n);
    if (A > 0)
        printf("%d is positive.", n);
    else if (A < 0)
        printf("%d is negative.",n);
    else if (A == 0)
        printf("%d is zero.", n);
    return 0;
}
