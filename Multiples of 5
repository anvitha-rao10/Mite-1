#include <stdio.h>
int main() {
    int num;
    printf("Enter an integer: ");
    scanf("%d", &num);
    if(num % 5 == 0)
        printf("%d is multiple of 5.", num);
    else
        printf("%d is not a multiple of 5.", num);
    return 0;
}
